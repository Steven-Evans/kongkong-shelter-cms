{
  "routes": [
    {
      "method": "GET",
      "path": "/postings",
      "handler": "posting.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/postings/count",
      "handler": "posting.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/postings/:id",
      "handler": "posting.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/postings",
      "handler": "posting.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/postings/:id",
      "handler": "posting.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/postings/:id",
      "handler": "posting.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
